<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is permitted ONLY for local development hosts.

  Android blocks cleartext by default, and 10.0.2.2 (the emulator's route to
  the developer machine) is where this app is built against. Every other
  destination — including the production Raut server — must be HTTPS, which
  is what the default config below enforces.

  Do not add production hostnames to the cleartext list. If a deployed server
  is reachable over plain HTTP, the fix is a certificate on that server, not
  an exemption here: field reps carry customer balances and payment records.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>
